// test/sanity.spec.js
// Agrupo las pruebas bajo el nombre "Sanity"
describe("Sanity", () => {
  // Caso básico para verificar que todo funciona
  it("1 + 1 = 2", () => {
    // Espero que la suma de 1 + 1 sea igual a 2
    expect(1 + 1).toBe(2);
  });
});

